# CLAUDE.md

This file is retained for compatibility with the upstream repository. The Codex-native
project guidance lives in `AGENTS.md`.

For this port, use:

- `.codex-plugin/plugin.json` for the Codex plugin manifest
- `.mcp.json` for optional MCP servers
- `commands/` for Codex slash-command workflows
- `skills/` for Codex skills
- `AGENTS.md` for repository guidance
