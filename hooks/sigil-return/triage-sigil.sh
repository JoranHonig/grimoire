#!/usr/bin/env bash
# triage-sigil.sh - Legacy compatibility shim.
#
# Codex-native Grimoire triages sigil findings explicitly in the summon/checks workflows.
# The original plugin used a post-tool hook here, but the Codex manifest does not register
# this script.

exit 0
